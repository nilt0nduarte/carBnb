<div class="container mt-5">
  <h1>My Bookings</h1>

  <% @bookings.each do |booking| %>
  <%= booking.start_date %>
  <%= booking.user_id %>
  <%= button_to "Delete this booking", booking, method: :delete if policy(@bookings).destroy? %>
  <% end %>

</div>
